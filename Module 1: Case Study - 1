# Task 1: Create a list containing squares of numbers from 1 to 10 (using List Comprehension)
square = [x**2 for x in range (1,11)]
print("list of squares from 1 to 10:",square)

# Task 2: Write a function to check if the year number is a leap year
def is_leap_year(year):
  if(year % 4 == 0 and year % 100 !==0) or (year % 400 == 0):
    return True
  return Fasle

# Task 3: Write a function to take an array and return another array that contains the members of the first array that are even
def get_even_numbers(arr):
  return [num for num in arr if num % 2 == 0]
# Test the get_even_numbers function
print("even numbers in [ 1,2,3,4,5,6]:",get_even_numbers([1,2,3,4,5,6]))

# Task 4: Write a function that takes 2 arrays and prints the members of the first array that are present in the second array
def common_elements(arr1, arr2):
  common = [elem for elem in arr1 if elem in arr2]
  print("common elements:", common)
common_elements([1,2,3,4,5], [2,3,4,5,6])  
